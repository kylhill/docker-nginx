---
name: Renovate

on:
  schedule:
    - cron: "32 9 * * 1"
  workflow_dispatch:

enable-email-notifications: true
   
jobs:
  renovate:
    runs-on: oci-deps
    container:
      image: docker.io/renovate/renovate:latest@sha256:e66b19362fdea02ca95d3947b0f4feb846453885d145b136cb8803b6d9224763
      volumes:
        - /opt/renovate-cache:/tmp/renovate/containerbase
    env:
      LOG_LEVEL: info
      RENOVATE_CONTAINERBASE_DIR: /tmp/renovate/containerbase
      RENOVATE_ENDPOINT: https://git.tacomafia.net/api/v1
      RENOVATE_GITHUB_COM_TOKEN: ${{ secrets.RENOVATE_GITHUB_COM_TOKEN }}
      RENOVATE_HOST_RULES: '[{"hostType":"forgejo","matchHost":"https://git.tacomafia.net/","allowInternal":true}]'
      RENOVATE_INTERNAL_HOST_ACCESS: block
      RENOVATE_ONBOARDING: "false"
      RENOVATE_PLATFORM: forgejo
      RENOVATE_REPOSITORIES: kylhill/docker-nginx
      RENOVATE_REQUIRE_CONFIG: required
      RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
    steps:
      - name: Update dependencies
        run: renovate
