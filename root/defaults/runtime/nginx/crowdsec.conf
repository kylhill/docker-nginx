lua_package_path '/usr/local/lua/crowdsec/?.lua;/usr/share/lua/common/?.lua;/usr/share/lua/common/?/init.lua;;';
lua_package_cpath '/usr/local/lib/lua/5.1/?.so;;';
lua_shared_dict crowdsec_cache 50m;
lua_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;

init_by_lua_block {
    local config_path = "/run/crowdsec/crowdsec-nginx-bouncer.conf"
    local config_file = io.open(config_path, "r")
    if config_file == nil then
        crowdsec_enabled = false
        ngx.log(ngx.WARN, "[Crowdsec] File " .. config_path .. " doesn't exist, bouncer disabled")
        return
    end
    config_file:close()

    crowdsec_enabled = true
    cs = require "crowdsec"
    local bouncer_version = require "bouncer_version"
    local ok, err = cs.init(config_path, "crowdsec-nginx-bouncer/v" .. bouncer_version)
    if ok == nil then
        ngx.log(ngx.ERR, "[Crowdsec] " .. err)
        error()
    end
    ngx.log(ngx.INFO, "[Crowdsec] Initialisation done")
}

map $server_addr $unix {
    default  0;
    "~unix:" 1;
}

access_by_lua_block {
    if not crowdsec_enabled then
        return
    end

    local cs = require "crowdsec"
    if ngx.var.unix == "1" then
        -- skip Unix socket requests (internal upstreams)
        ngx.log(ngx.DEBUG, "[Crowdsec] Unix socket request, skipping...")
    elseif ngx.var.addr_allowed == "yes" then
        -- skip trusted LAN/VPN/Docker clients - CrowdSec bans never apply to these IPs
        ngx.log(ngx.DEBUG, "[Crowdsec] Trusted network client, skipping...")
    else
        cs.Allow(ngx.var.remote_addr)
    end
}

init_worker_by_lua_block {
    if not crowdsec_enabled then
        return
    end

    cs = require "crowdsec"
    local mode = cs.get_mode()
    if string.lower(mode) == "stream" then
        ngx.log(ngx.INFO, "Initializing stream mode for worker " .. tostring(ngx.worker.id()))
        cs.SetupStream()
    end

    if ngx.worker.id() == 0 then
        ngx.log(ngx.INFO, "Initializing metrics for worker " .. tostring(ngx.worker.id()))
        cs.SetupMetrics()
    end
}
