# Permissive defaults for deployments that do not provide GeoIP rules.
# Override this file at /config/nginx/http.d/geoip2.conf to enforce country
# or trusted-network access controls.
map $remote_addr $addr_allowed {
    default no;
}

map $remote_addr $access_allowed {
    default yes;
}
