# Conservative rate limiting - safety net against abuse, not strict throttling
# Zone: 10MB shared memory, 100 req/s per IP average (burst 200)
limit_req_zone $binary_remote_addr zone=general:10m rate=100r/s;
limit_req_status 429;

# Apply conservative rate limiting with high burst allowance
limit_req zone=general burst=200 nodelay;
