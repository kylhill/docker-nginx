## Version 2026/07/20 - Review configuration changes before adopting updates.
# Disable this header for internal names, self-signed certificates, or any
# deployment where clients may need to return to HTTP before max-age expires.
add_header Strict-Transport-Security "max-age=63072000" always;
