## Version 2026/07/20 - Review configuration changes before adopting updates.
# This sample shows the fallback value. On first startup, the active file is
# generated from the nameserver entries in the container's /etc/resolv.conf.
resolver 127.0.0.11 ipv6=off valid=300s;
resolver_timeout 2s;
