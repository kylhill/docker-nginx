# Safe shared-proxy default.
# Leave framing, referrer, and permissions policies to each upstream app so
# the proxy does not weaken stricter application-specific headers.
add_header X-Content-Type-Options "nosniff" always;
