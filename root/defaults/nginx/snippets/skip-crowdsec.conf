# Skip CrowdSec WAF checks for this location.
# A location-level access_by_lua_block overrides the http-level block in crowdsec.conf.
# Include this in any location that should bypass IP reputation and AppSec inspection.
access_by_lua_block { return }
